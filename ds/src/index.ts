export const THEME = {
    primaryColor: '#0070f3',
    backgroundColor: '#f5f5f5',
    borderRadius: '8px',
    padding: '24px'
};

export function getWelcomeMessage(appName: string): string {
    return `Hello from Design-System to app ${appName}!`;
};
